server:
  port: 8888
  
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sunmathi19s/spring-cloud-confg-server.git
          search-paths:
          - '{application}'
     
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"     
   